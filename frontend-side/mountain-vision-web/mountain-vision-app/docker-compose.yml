version: '3.9'

services:
  web:
    image: project-silent-ascent
    container_name: mountain_vision_dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    command: pnpm dev
